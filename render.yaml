services:
  - type: web
    name: drf_api
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn drf_api.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: my-database
          property: connectionString
